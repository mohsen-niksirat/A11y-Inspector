{
  "name": "a11y-inspector",
  "version": "1.2.0",
  "description": "Privacy-first, browser-only HTML accessibility audit tool with sandboxed preview, safe fixes, and multilingual UI.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/engine.test.mjs tests/cli.test.mjs",
    "check": "node --check app.js && node --check cli.mjs",
    "verify": "node --check app.js && node --check cli.mjs && node --test tests/engine.test.mjs tests/cli.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mohsen-niksirat/A11y-Inspector.git"
  },
  "keywords": [
    "accessibility",
    "a11y",
    "wcag",
    "audit",
    "browser",
    "privacy",
    "static-site"
  ],
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
